services:
  - type: worker
    name: ultimate-hosting-bot
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python ultimate_hosting_bot_fixed.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
